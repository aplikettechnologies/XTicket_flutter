import 'package:flutter/material.dart';

class NearmeScreen extends StatelessWidget {
  const NearmeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Near me"));
  }
}
